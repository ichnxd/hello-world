@inherits System.Web.Mvc.WebViewPage
@using System.Web.Mvc;
@{
    Layout = null;
}
@Html.JsMinify(@<text>
//<script>
    define(['app', 'baseView'], function (app, BaseClass) {

        var CalendarViewController = function ($scope, CurrentObject, ViewStateName, IsModal, ModalMode) {
            var _ = this;
            _.Init($scope, CurrentObject, ViewStateName, IsModal, ModalMode);

            //
            //
            //

            $scope.CalendarOption = {
                width: '100%',
                height: '100%'
            };

        }

        CalendarViewController.prototype = Object.create(BaseClass.prototype);

        CalendarViewController.$inject = ['$scope', 'CurrentObject', 'ViewStateName', 'IsModal', 'ModalMode'];

        app_cached_providers.$controllerProvider.register('CalendarViewController', CalendarViewController);

    });
//</script>
</text> , false )